export type createGamePayload = {
    creatorId: number
    couponId: number
    question: string
    answer: string
}

export type gameResponse = {
    creatorId: number
    couponId: number
    question: string
    answer: string
}